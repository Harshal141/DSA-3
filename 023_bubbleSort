// n is size of array (arr)

for(int i=n-1;i>0;--i){
  bool testSwap = false;
  for(int j=0;j<i;++j){
    if(arr[j]>arr[j+1]){
      swap(arr[j],arr[j+1]);
      testSwap = true;
    }
  }
  if(testSwap == false)break;
}